# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/

# Environment / secrets — never commit
.env
.env.*
!.env.example
venv/
.venv/

# PHI / data / outputs — Minerva-local; never committed
data/
outputs/

# Local context documents (manuscripts, peer reviews, data shells) —
# reference material only; never pushed to GitHub
*.docx
*.pdf
*.xlsx

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
.split_salt

# Run artifacts derived from the cohort — generated on-platform, never committed
split_manifest_public.csv
metadata_EVAL_v1.0.json
